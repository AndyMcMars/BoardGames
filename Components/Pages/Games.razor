@page "/"
@using BoardGames.Components.Pages.Icons
@using LumexUI.Common
@using LumexUI.Shared.Icons
@using Microsoft.EntityFrameworkCore;
@using System.Globalization

@inject NavigationManager Nav
@inject AppDbContext Db

<LumexTextbox Placeholder="@SharedResource.Games_SearchByName"
              Class="max-w-sm"
                Behavior="@InputBehavior.OnInput"
                @bind-Value="@_nameFilter">
    <StartContent>
        <SearchIcon Size="20" />
    </StartContent>
</LumexTextbox>

<div class="grid grid-cols-2 lg:grid-cols-4 gap-4 mt-4 justify-items-center">

    @foreach (var game in FilteredGames)
    {
        <LumexCard onclick="@(() => GoToMatches(game.Slug))" Class="w-full max-w-sm">
            <div class="w-full aspect-square overflow-hidden">
                <img src="@game.Image"
                     class="w-full h-full object-cover object-top" />
            </div>
            <LumexCardFooter Blurred="@true">
                <div class="flex justify-between w-full">
                    <h2>@game.Name</h2>
                    <LumexChip Variant="ChipVariant.Light">
                        <StartContent>
                            @_matches.Where(m=>m.GameId == game.Id).Count()
                        </StartContent>
                        <ChildContent>
                            <SwordsIcon/>
                        </ChildContent>
                    </LumexChip>
                </div>
            </LumexCardFooter>
        </LumexCard>
    }
</div>

@code {

    private IQueryable<mdGame> _games => Db.Games;
    private IQueryable<mdMatch> _matches => Db.Matches;

    private IQueryable<mdGame> FilteredGames
    {
        get
        {
            var result = _games;

            if (!string.IsNullOrEmpty(_nameFilter))
            {
                result = result.Where(u => EF.Functions.Like(u.Name, $"%{_nameFilter}%"));
            }

            return result;
        }
    }

    private string? _nameFilter;

    private void GoToMatches(string game_slug)
    {
        Nav.NavigateTo($"/{game_slug}");
    }
}
